/**
 * Maid of Threads — Main JavaScript entry point
 *
 * CSS-only entry point for Vite/Tailwind. HTMX is loaded via CDN
 * in _main.php. All UI interactions use HTMX or vanilla JS.
 */

import './app.css';
